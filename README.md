# CRUD Products API

یک پروژه ساده برای پیاده‌سازی عملیات CRUD (Create, Read, Update, Delete)  
با استفاده از **Node.js، Express، TypeScript و MongoDB**

---

## 🎯 هدف پروژه
ساخت یک REST API برای مدیریت محصولات که بتواند:
- محصول جدید ایجاد کند
- لیست تمام محصولات را برگرداند
- اطلاعات یک محصول خاص را نمایش دهد
- اطلاعات محصول را ویرایش کند
- محصول را حذف کند

---

## 🛠️ تکنولوژی‌های استفاده‌شده
- Node.js (نسخه 16 به بالا)
- Express.js
- TypeScript
- MongoDB
- Mongoose
- ts-node-dev
- Thunder Client (برای تست API)

---

## 📁 ساختار پروژه
```
project-crud/
│
├── src/
│   ├── models/
│   ├── controllers/
│   ├── routes/
│   ├── config/
│   └── server.ts
│
├── .env
├── package.json
├── tsconfig.json
└── README.md
```

---

## ⚙️ نصب و اجرا

### 1️⃣ نصب وابستگی‌ها
```bash
npm install
```

### 2️⃣ تنظیم متغیرهای محیطی
فایل `.env` بسازید:
```env
PORT=5000
MONGO_URI=mongodb://localhost:27017/productdb
```

### 3️⃣ اجرای پروژه
```bash
npm run dev
```

---

## 🔗 API Endpoints

| Method | Endpoint | توضیح |
|------|---------|------|
| GET | /api/products | دریافت همه محصولات |
| GET | /api/products/:id | دریافت یک محصول |
| POST | /api/products | ایجاد محصول جدید |
| PUT | /api/products/:id | ویرایش محصول |
| DELETE | /api/products/:id | حذف محصول |

---

## 🧪 تست API
برای تست API از **Thunder Client** یا Postman Desktop استفاده شده است.

### نمونه Body برای ایجاد محصول:
```json
{
  "name": "لب تاپ",
  "description": "لپ تاپ ایسوس",
  "price": 1200000,
  "stock": 5
}
```

